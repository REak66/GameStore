PORT=5050
MONGODB_URI=mongodb://localhost:27017/shopping_db
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRE=1h
CLIENT_URL=http://localhost:4200

# ── Gmail API (OAuth2) ──────────────────────────────────────────────────────
# 1. Go to https://console.cloud.google.com → APIs & Services → Credentials
# 2. Create OAuth 2.0 Client ID (Desktop app)
# 3. Enable the Gmail API for the project
# 4. Visit https://developers.google.com/oauthplayground
#    → Gear icon → check "Use your own OAuth credentials" and paste your IDs
#    → Scope: https://mail.google.com/
#    → Authorise → Exchange auth code for tokens → copy Refresh Token
GMAIL_CLIENT_ID=your_client_id.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your_client_secret
GMAIL_REFRESH_TOKEN=your_refresh_token
GMAIL_FROM="GameShop <your_email@gmail.com>"

# ── Google Gemini AI (Help Center AI Chat) ─────────────────────────────────
# 1. Go to https://aistudio.google.com/app/apikey
# 2. Create an API key (free tier available)
# 3. Paste it below
GEMINI_API_KEY=AIzaSyDuzdSgFyf9Hbn3Dk4szmUvC9qiPGCc35U

# ── Groq AI (Help Center AI Chat — Free fallback) ──────────────────────────
# 1. Go to https://console.groq.com/keys
# 2. Create a free API key (no credit card needed)
# 3. Paste it below — used as fallback if Gemini is unavailable
GROQ_API_KEY=your_groq_api_key_here
